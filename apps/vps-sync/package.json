{
  "name": "@airing-cal/vps-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json && esbuild dist/upstream/fetch.js dist/upstream/retry.js dist/run.js dist/media/refresh.js dist/postgres/repositories.js --bundle --splitting --allow-overwrite --format=esm --platform=node --target=node20 --outdir=dist --outbase=dist && cp -R src/postgres/migrations dist/postgres/migrations",
    "build:check": "tsc --noEmit",
    "test": "tsx --test --test-skip-pattern=PostgreSQL",
    "test:integration": "tsx --test src/postgres/postgres.integration.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@airing-cal/bgm-api": "workspace:*",
    "@airing-cal/domain": "workspace:*",
    "@aws-sdk/client-s3": "3.1124.0",
    "@sentry/node": "10.73.0",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "@types/pg": "^8.23.1",
    "esbuild": "0.28.2"
  }
}
