{
  "name": "@airing-cal/vps-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc --outDir dist && rm dist/postgres/migrate.test.js && cp -R src/postgres/migrations dist/postgres/migrations",
    "build:check": "tsc --noEmit",
    "test": "tsx --test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "@types/pg": "^8.23.1"
  }
}
